spring:
  profiles:
    active: prod
  application:
    name: mall-admin
  cloud:
    nacos:
      discovery:
        server-addr: nacos-registry
      config:
        server-addr: nacos-registry
        file-extension: yaml